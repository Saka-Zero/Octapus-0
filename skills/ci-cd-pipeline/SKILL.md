<!--
  Bundled with Octapus-0
  Source: original (Octapus-0)
-->

---
name: ci-cd-pipeline
description: "Design, build, and debug CI/CD pipelines — GitHub Actions, GitLab CI, Jenkins. Use when the user mentions 'CI', 'CD', 'pipeline', 'github actions', 'gitlab ci', 'jenkins', 'workflow yaml', 'build failing', 'deploy stage', 'runner', 'artifact', 'continuous integration', or a job/stage in their pipeline is failing."
keywords: ci cd, pipeline, github actions, gitlab ci, jenkins, continuous integration, continuous deployment, workflow yaml, build failing, runner, deploy
---

# ci-cd-pipeline

Apply this methodology when activated. Be concrete and structured: exact commands, code, or templates — never vague advice. Adapt depth to the user's context.